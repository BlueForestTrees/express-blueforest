{
  "name": "express-blueforest",
  "version": "1.1.5",
  "main": "src/express-blueforest.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "cross-env NODE_ENV=test mocha --recursive --exit \"test/**/*.js\"",
    "build": "rimraf dist && babel src -d dist",
    "prepublishOnly": "npm version patch && npm run build",
    "npmpublish": "npm publish"
  },
  "description": "lib pour créer des micro-apis basé sur express et json",
  "license": "MIT",
  "dependencies": {
    "body-parser": "1.18.3",
    "express": "4.16.4",
    "express-validator": "5.3.1",
    "fs-readdir-recursive": "^1.1.0",
    "morgan": "1.9.1"
  },
  "devDependencies": {
    "chai": "4.2.0",
    "cross-env": "5.2.0",
    "mocha": "6.0.2",
    "rimraf": "2.6.3"
  }
}
